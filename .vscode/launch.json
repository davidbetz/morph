{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Go: Run counts",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/counts",
            "args": ["-mode", "gnt", "-target", "text"],
            "cwd": "${workspaceFolder}/cmd/counts"
        }
    ]
}
